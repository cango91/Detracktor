{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://detracktor.xn--glolu-jua30a.com/schema/v1/warnings.schema.json",
    "title": "Detracktor Warning Settings",
    "description": "JSON schema for warning-related settings",
    "type": "object",
    "properties": {
      "warnOnEmbeddedCredentials": {
        "type": "boolean",
        "description": "Whether to warn when credentials are embedded in URLs",
        "default": true
      },
      "sensitiveParams": {
        "type": "array",
        "description": "Parameter name patterns considered sensitive (decoded names)",
        "items": {
          "type": "string",
          "minLength": 1
        }
      },
      "sensitiveMerge": {
        "type": "string",
        "enum": ["UNION", "REPLACE"],
        "description": "How to combine with lower-priority sensitiveParams: UNION merges; REPLACE overrides",
        "default": "UNION"
      }
    },
    "additionalProperties": false
  }

